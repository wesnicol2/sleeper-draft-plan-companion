[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "python-service-template"
version = "0.1.0"
description = "A containerized Python service"
requires-python = ">=3.11"
# Runtime dependencies live in requirements.txt -- that is what the Dockerfile
# installs. Listing them here too would just give them somewhere to drift apart.

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.16",
]

[tool.setuptools]
# Flat layout: be explicit, or setuptools auto-discovery trips over tests/.
packages = ["app"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # import sorting
    "UP",   # pyupgrade
    "B",    # bugbear
    "C4",   # comprehensions
    "SIM",  # simplify
    "PERF", # performance
    "RUF",  # ruff-specific
]
ignore = [
    # The formatter owns line length; flagging it again just adds noise.
    "E501",
]

[tool.ruff.lint.per-file-ignores]
# Tests bind fixtures and intermediate values that exist for readability.
"tests/*" = ["RUF059"]

[tool.pytest.ini_options]
testpaths = ["tests"]
